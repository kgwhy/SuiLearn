<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Production defaults to HTTPS-only (cleartext disabled). Cleartext HTTP is permitted
        ONLY for the local development hosts below: the emulator loopback (10.0.2.2) and
        localhost. Point SUILEARN_API_BASE_URL at an https:// host for any non-dev build.
    -->
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
