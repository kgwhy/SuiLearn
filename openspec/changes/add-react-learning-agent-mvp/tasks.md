## 1. 兼容性与契约门禁

- [ ] 1.1 在任何 Agent 业务实现前，通过最小 disabled/enabled Application Context 测试验证 Spring AI/Spring AI Alibaba 候选版本兼容。负责人：Server Backend Agent。允许文件：`services/api/pom.xml`、`services/api/src/test/java/com/suilearn/api/agent/compatibility/**`、`openspec/changes/add-react-learning-agent-mvp/**`。禁止文件：`apps/**`、`contracts/**`、`docs/**`、所有非 Agent 生产源码。测试命令：`mvn -f services/api/pom.xml -Dtest=AgentFrameworkCompatibilityTest test -q`。审查重点：保持 Java 21 与 Spring Boot 3.5.14；不引入未经审查的 dependency exclusion；disabled Context 仍可启动；失败必须回到 Spec，不能静默更换框架。

- [ ] 1.2 定义同步 Agent 运行与学习者记忆删除的增量 OpenAPI 契约，包括稳定成功/错误 schema、预算/action 元数据与单用户 learner/session 语义。负责人：Architect Agent。允许文件：`contracts/openapi/suilearn-v2.yaml`、`openspec/changes/add-react-learning-agent-mvp/**`。禁止文件：`services/**`、`apps/**`、`docs/product-requirements.md`、`docs/tech-selection.md`。测试命令：`python -c "import pathlib,yaml; yaml.safe_load(pathlib.Path('contracts/openapi/suilearn-v2.yaml').read_text(encoding='utf-8')); print('OK')"`。审查重点：增量 `/api/v2` 契约；不得出现思维链字段；scope、错误与降级状态符合规格；不得声称认证或多租户能力。

## 2. Context、Prompt 与记忆基础

- [ ] 2.1 为 ContextAssembler、按优先级裁剪的 ContextBudgetPolicy、SubAgent 最小快照、不可信 evidence、PromptRegistry allowlist/version/hash 与一次结构修复编写先失败的单元测试。负责人：Server Backend Agent。允许文件：`services/api/src/test/java/com/suilearn/api/agent/context/**`、`services/api/src/test/java/com/suilearn/api/agent/prompt/**`、`services/api/src/test/resources/agents/**`、`openspec/changes/add-react-learning-agent-mvp/**`。禁止文件：生产源码、`apps/**`、`contracts/**`、稳定文档。测试命令：`mvn -f services/api/pom.xml -Dtest='*AgentContext*,*PromptRegistry*,*StructuredAgentOutput*' test -q`。审查重点：测试必须先因缺失行为失败，断言固定优先级和无正文泄露，且不依赖真实 AI/网络。

- [ ] 2.2 实现类型化 Agent 配置、ContextManager 与版本化 Prompt Registry，使任务 2.1 测试通过。负责人：Server Backend Agent。允许文件：`services/api/src/main/java/com/suilearn/api/agent/config/**`、`services/api/src/main/java/com/suilearn/api/agent/context/**`、`services/api/src/main/java/com/suilearn/api/agent/prompt/**`、`services/api/src/main/resources/agents/**`、`services/api/src/main/resources/application.properties`、对应测试、`openspec/changes/add-react-learning-agent-mvp/**`。禁止文件：既有 AI/RAG 实现（仅可经公开 port 使用）、`apps/**`、`contracts/**`、稳定文档。测试命令：任务 2.1 命令加 `mvn -f services/api/pom.xml -Dtest=AgentConfigurationPropertiesTest test -q`。审查重点：固定资源 allowlist、类型安全变量、精确配置校验、不可变 system/scope context 与有界的一次修复。

- [ ] 2.3 为 working、Redis session、PostgreSQL/pgvector semantic memory 编写先失败的单元/集成测试，覆盖 TTL、最大 turn、learner 隔离、允许类型、置信度/来源晋升、指纹去重、embedding 降级与删除。负责人：Server Backend Agent。允许文件：`services/api/src/test/java/com/suilearn/api/agent/memory/**`、`services/api/src/test/resources/**`、`openspec/changes/add-react-learning-agent-mvp/**`。禁止文件：生产源码、`apps/**`、`contracts/**`、稳定文档。测试命令：`mvn -f services/api/pom.xml -Dtest='*AgentMemory*,*MemoryPromotion*' test -q`。审查重点：Testcontainers 或确定性 store 证明 Redis/PostgreSQL 语义；不得读取其他 learner 数据；原始 transcript 不得被断言为持久记忆。

- [ ] 2.4 实现 MemoryManager、Redis session adapter、semantic-memory entity/repository/vector recall、晋升策略与定向删除，使任务 2.3 测试通过。负责人：Server Backend Agent。允许文件：`services/api/src/main/java/com/suilearn/api/agent/memory/**`、`services/api/src/main/java/com/suilearn/api/agent/infrastructure/memory/**`、对应测试、`services/api/pom.xml`、`openspec/changes/add-react-learning-agent-mvp/**`。禁止文件：既有 knowledge/material/question persistence 源码、`apps/**`、`contracts/**`、稳定文档。测试命令：任务 2.3 命令。审查重点：先按 learner 过滤再相似度检索；Redis key 受控；模型不能直接写数据库；失败/删除状态诚实；schema/index 初始化幂等。

## 3. ReAct 与 SubAgent 实现

- [ ] 3.1 为 EvidenceBundle 工具、scope 强制、Supervisor 路由、Agent-as-Tool 契约、共享预算、无证据停止、临时练习输出与禁止 action 编写先失败测试。负责人：Server Backend Agent。允许文件：`services/api/src/test/java/com/suilearn/api/agent/tool/**`、`services/api/src/test/java/com/suilearn/api/agent/orchestration/**`、`openspec/changes/add-react-learning-agent-mvp/**`。禁止文件：生产源码、`apps/**`、`contracts/**`、稳定文档。测试命令：`mvn -f services/api/pom.xml -Dtest='*LearningAgent*,*AgentTool*,*SubAgent*,*AgentBudget*' test -q`。审查重点：确定性 fake model；Supervisor 不得绕过 evidence；SubAgent 只拥有最小工具/上下文；必须断言最大 step/tool call/timeout。

- [ ] 3.2 实现 LearningAgentPort、只读 evidence 工具、KnowledgeResearchSubAgent、PracticeCoachSubAgent 与 Spring AI Alibaba Supervisor adapter，使任务 3.1 测试通过。负责人：Server Backend Agent。允许文件：`services/api/src/main/java/com/suilearn/api/agent/application/**`、`services/api/src/main/java/com/suilearn/api/agent/tool/**`、`services/api/src/main/java/com/suilearn/api/agent/infrastructure/springai/**`、对应测试与 Prompt 资源、`openspec/changes/add-react-learning-agent-mvp/**`。禁止文件：`QuestionStore`、`GeneratedContentStore`、任务派发代码、`apps/**`、`contracts/**`、稳定文档。测试命令：任务 3.1 命令。审查重点：Spring 框架类型仅位于 infrastructure；固定两个 SubAgent；不允许嵌套 Agent；练习仅存在于响应；不得宽泛动态注册工具。

- [ ] 3.3 为运行和记忆删除端点、校验、disabled/model/Redis/scope/invalid-output 错误、响应脱敏与低基数指标编写先失败的 Controller/OpenAPI 契约测试。负责人：Server Backend Agent。允许文件：`services/api/src/test/java/com/suilearn/api/agent/controller/**`、`services/api/src/test/java/com/suilearn/api/agent/metrics/**`、`openspec/changes/add-react-learning-agent-mvp/**`。禁止文件：生产源码、`apps/**`、稳定文档。测试命令：`mvn -f services/api/pom.xml -Dtest='*LearningAgentController*,*LearningAgentOpenApiContract*,*AgentMetrics*' test -q`。审查重点：与 OpenAPI 精确一致；HTTP/status 稳定；无思维链/Prompt/原始响应字段；无高基数 metric tag。

- [ ] 3.4 实现 Agent Controller/DTO/error mapping 与 Micrometer 指标，使任务 3.3 测试通过。负责人：Server Backend Agent。允许文件：`services/api/src/main/java/com/suilearn/api/agent/controller/**`、`services/api/src/main/java/com/suilearn/api/agent/metrics/**`、对应测试、`openspec/changes/add-react-learning-agent-mvp/**`。禁止文件：既有无关 Controller、`apps/**`、`contracts/**`、稳定文档。测试命令：任务 3.3 命令。审查重点：增量 `/api/v2`；Bean Validation 匹配契约；disabled/unavailable/degraded 语义明确；日志与 tag 不得包含正文或 learner/session ID。

## 4. Eval 与运行态验证

- [ ] 4.1 按设计矩阵增加 Agent/Redis 配置默认值和本地 Compose 接线，保持 Agent 默认 disabled 且不包含生产密钥。负责人：Architect Agent，经 Leader 授权修改根运行时文件。允许文件：`compose.yml`、`.env.example`、`services/api/config/local.properties.example`、`services/api/src/main/resources/application.properties`、`openspec/changes/add-react-learning-agent-mvp/**`。禁止文件：`apps/**`、`contracts/**`、稳定文档、业务 Java 源码。测试命令：`docker compose --env-file .env.example config` 与 `mvn -f services/api/pom.xml -Dtest=AgentConfigurationPropertiesTest test -q`。审查重点：所有默认值/覆盖/范围一致；Redis 故障显式；无重复或旧 Agent key；无密钥。

- [ ] 4.2 增加不少于 10 个确定性 Agent Eval 场景，覆盖路由、scope、citation、无证据、预算、无效 schema、session/semantic memory、context 裁剪与 Prompt Injection。负责人：Test Agent。允许文件：`services/api/src/test/java/com/suilearn/api/agent/eval/**`、`services/api/src/test/resources/agent-eval/**`、`openspec/changes/add-react-learning-agent-mvp/**`。禁止文件：生产源码、`apps/**`、`contracts/**`、稳定文档。测试命令：`mvn -f services/api/pom.xml -Dtest='*AgentEval*' test -q`。审查重点：fresh 的确定性 fixture；不访问真实 model/network；每个场景都有清晰断言和通过/失败计数。

- [ ] 4.3 执行运行态验收矩阵：disabled 启动、enabled + PostgreSQL/Redis/fake AI、Redis 中断、AI 缺失、非法配置、两个 learner/session 隔离、关键词/vector 路径、HTTP 响应、记忆删除和指标。负责人：Test Agent。允许文件：`services/api/src/test/**`、`services/api/target/**`、`openspec/changes/add-react-learning-agent-mvp/verification.md`。禁止文件：生产源码和稳定文档。测试命令：目标 Agent 测试、`mvn -f services/api/pom.xml test -q`、`docker compose --env-file .env.example config` 与文档化的本地运行态探针。审查重点：真实启动证据；失败路径不得伪造成功；Android/Web/无关 Backend 回归不受影响。

## 5. 稳定事实、审查与收口

- [ ] 5.1 仅同步已验证的产品事实，包括学习 Agent 用户边界、仅 API MVP、临时练习语义与明确非目标。负责人：Product Agent。允许文件：`docs/product-requirements.md`、`openspec/changes/add-react-learning-agent-mvp/**`。禁止文件：`docs/proposals/**`、架构/技术文档、实现源码、`apps/**`。测试命令：`openspec validate add-react-learning-agent-mvp --strict` 加 policy 中的产品范围残留扫描。审查重点：事实必须来自已验证行为而非目标描述；不得扩展为 Web/account/platform。

- [ ] 5.2 仅同步已验证的架构与技术事实，包括模块边界、确切兼容依赖版本、Redis 语义、配置矩阵与 API 数据流。负责人：Architect Agent。允许文件：`docs/architecture.md`、`docs/tech-selection.md`、`openspec/changes/add-react-learning-agent-mvp/**`。禁止文件：`docs/proposals/**`、产品文档、实现源码、`apps/**`。测试命令：`openspec validate add-react-learning-agent-mvp --strict` 加 policy 中的依赖/配置残留扫描。审查重点：稳定事实只能记录已验证运行态与确切版本；失败候选不得写成当前能力。

- [ ] 5.3 先进行独立 Spec Review，再进行 Code Review；将每个 P0/P1/P2 发现路由给负责 Agent 做定向修复/复测。负责人：Reviewer Agent。允许文件：完整 change 的只读审查；仅可在 `openspec/changes/add-react-learning-agent-mvp/**` 记录发现。禁止文件：直接修改生产/测试代码。测试命令：检查 `git diff 6f1434ef849bd8e467cc8e0e1c68c37fa9e998c4 --stat`、受影响 diff 与 Test Agent 证据。审查重点：需求/契约一致、角色/文件边界、依赖泄漏、记忆隔离、注入/scope、伪成功、测试诚实与不必要的平台能力。

- [ ] 5.4 在所有实现、测试和审查通过后完成 Verify 与 Archive 证据。负责人：Leader Agent。允许文件：`openspec/changes/add-react-learning-agent-mvp/verification.md`、`openspec/changes/add-react-learning-agent-mvp/archive.md`、`openspec/changes/add-react-learning-agent-mvp/tasks.md`。禁止文件：实现与稳定事实文件。测试命令：完整 policy 验收矩阵、`git diff --check`、`openspec validate add-react-learning-agent-mvp --strict`、工作流检查器和 `git diff 6f1434ef849bd8e467cc8e0e1c68c37fa9e998c4 --stat`。审查重点：没有陈旧 open/in-progress/无 owner pending 状态；所有文件符合 policy；稳定事实已同步；独立审查已关闭；不存在未解决 P0/P1/P2。
